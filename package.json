{
  "name": "iidx-score-attack-manager-web",
  "private": true,
  "version": "0.9.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @iidx/web-app dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "sync:versions": "node scripts/sync-versions.mjs",
    "sync:versions:check": "node scripts/sync-versions.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
