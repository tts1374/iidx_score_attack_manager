{
  "name": "iidx-score-attack-manager-web",
  "private": true,
  "version": "0.1.3",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @iidx/web-app dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
